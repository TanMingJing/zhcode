// React Hooks 简化测试

// 简单的 useState
令 [计数, 设置计数] = React.useState(0);

// useEffect
React.useEffect(() => {
  输出("mounted");
}, []);

// 在函数中使用 hooks
函数 计数器() {
  令 [计数, 设置计数] = React.useState(0);
  返回 <Button onClick={() => 设置计数(计数 + 1)}>
    计数: {计数}
  </Button>;
}
