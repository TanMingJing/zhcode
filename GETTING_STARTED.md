# 快速开始指南

## ✅ 已完成的设置

### 1. 项目结构
```
wencode/
├── packages/
│   ├── core/        ✅ 核心语言引擎
│   ├── cli/         ✅ 命令行工具
│   ├── ide/         ✅ Web IDE
│   ├── vscode-ext/  ✅ VS Code 扩展
│   └── ai-service/  ✅ AI 后端服务
├── docs/            ✅ 文档
├── examples/        ✅ 示例代码目录
└── .github/         ✅ CI/CD 工作流
```

### 2. 开发环境
- ✅ Git 仓库初始化
- ✅ pnpm Monorepo 配置
- ✅ TypeScript 配置（全项目）
- ✅ ESLint + Prettier 配置
- ✅ Vitest 测试框架
- ✅ GitHub Actions CI/CD 流程

### 3. 核心配置文件
- `package.json` - Monorepo 根配置
- `tsconfig.json` - TypeScript 统一配置
- `.eslintrc.json` - 代码检查规则
- `.prettierrc` - 代码格式化规则
- `vitest.config.ts` - 测试框架配置
- `.gitignore` - Git 忽略规则

---

## 🚀 下一步：启动 Phase 1 - 核心语言原型

### 任务 1.1：完成语言设计与关键字定义

#### 第一步：创建语言规范文档
在 `docs/` 目录下创建新文件：
- `docs/language-spec.md` - 完整的语言语法规范（BNF/EBNF）
- `docs/keywords.md` - 中文关键字完整映射表

#### 第二步：定义关键字与符号
在 `packages/core/src/` 下创建：
```typescript
// types/keywords.ts - 关键字定义
// types/tokens.ts - Token 类型定义
// constants/keyword-map.ts - 关键字映射表
```

#### 第三步：实现 Tokenizer
```bash
cd packages/core

# 创建 Tokenizer 模块
mkdir -p src/tokenizer
touch src/tokenizer/index.ts
touch src/tokenizer/tokenizer.ts
touch src/tokenizer/token.ts
```

#### 第四步：编写单元测试
```bash
# 为 Tokenizer 编写测试
touch src/tokenizer/tokenizer.test.ts
```

---

## 📋 推荐工作流

### 安装与启动
```bash
# 1. 安装 pnpm（如果未安装）
npm install -g pnpm

# 2. 进入项目目录
cd c:\Users\mjtan\Desktop\wencode

# 3. 安装所有依赖
pnpm install

# 4. 运行所有测试
pnpm test

# 5. 运行代码检查
pnpm lint
```

### 开发工作流
```bash
# 在 core 包中开发
cd packages/core

# 或在根目录运行 core 包专用命令
pnpm -F @wencode/core dev
pnpm -F @wencode/core test
pnpm -F @wencode/core build
```

---

## 📖 关键文档速览

| 文件 | 用途 |
|------|------|
| [context.md](docs/context.md) | 产品设计、特性、技术架构 |
| [plan.md](docs/plan.md) | 分阶段开发计划与检查清单 |
| [README.md](README.md) | 项目概览与快速开始 |

---

## 💡 Phase 1 具体步骤

### 1.1 语言设计与关键字定义
- [ ] 定义完整的中英文关键字映射
- [ ] 编写语法规范（BNF）
- [ ] 定义预留字与标识符规则
- [ ] 确定错误消息策略

### 1.2-1.4 实现 Tokenizer → Parser → Transpiler
这些将在 `packages/core/src/` 中实现：
```
src/
├── types/           # 类型定义
├── tokenizer/       # 词法分析
├── parser/          # 语法分析
├── transpiler/      # 代码生成
├── ast/             # AST 节点类型
└── utils/           # 工具函数
```

### 1.5 运行环境与测试
- 搭建 Vitest 测试框架（已完成基础设置）
- 编写单元测试
- 建立 CI/CD 流程（已配置 GitHub Actions）

---

## 🎯 立即开始

### 选项 A：快速原型（推荐）
1. 在 `docs/` 中创建 `language-spec.md`
2. 定义关键字映射（可先用 JSON 或常量）
3. 在 `core` 包中实现 Tokenizer 基础版本
4. 编写 10+ 单元测试

### 选项 B：详细规范优先
1. 完整编写语言规范文档
2. 定义完整的 AST 结构
3. 再开始实现 Tokenizer

### 建议时间估计
- 关键字定义与规范：2-3 天
- Tokenizer 实现：3-5 天
- Parser 实现：4-6 天
- Transpiler 实现：3-5 天
- **Phase 1 总计**：12-19 天（1 人独立开发）

---

## 💻 立即操作

要立即启动 Phase 1，请运行：

```bash
# 1. 进入项目目录
cd c:\Users\mjtan\Desktop\wencode

# 2. 进入 core 包
cd packages/core

# 3. 查看当前结构
ls -la src/

# 4. 开始编辑 src/index.ts 进行开发
```

或者让我帮你直接创建 Phase 1.1 的文件和框架！

---

**现在可以开始 Phase 1 的第一步吗？需要我帮助创建语言规范文档和关键字定义吗？**
